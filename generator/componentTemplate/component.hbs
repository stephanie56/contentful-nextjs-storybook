import React from 'react';

interface {{properCase name}}Props {
  /**
   * the type definition will be used in the storybook controls
   */
  heading?: string;
}

/**
 * the component description will be used in the storybook docs
 */
export const {{ properCase name }} = ({
  heading,
  ...props
}: {{properCase name}}Props) => {
  return (
    <>
      <div> {{ properCase name }} heading: { heading } </div>
    </>
  );
};
